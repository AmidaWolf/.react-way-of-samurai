let initialState = {
    onlineFriends: [
        {id: 0, name: 'Kus Kabanov'},
        {id: 1, name: 'Gus Kabanov'},
    ]
}

export const navigationReducer = (state = initialState, action) => {
    return state;
}
